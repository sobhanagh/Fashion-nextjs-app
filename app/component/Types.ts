export type imageType = {
    srcBackground: string;
    srcPerson: string;
    title: string;
    price: string;
};

export type imageTypeDesigner = {
    srcBackground: string;
    srcPerson: string;
    title: string;
    desc: string;
};